<div class="tab {{tab.cssClass}}" data-group="primary" data-tab="description">
  <div class="stat-name bold">{{localize 'PEN.description'}}:</div>
  <div class="background-doc">
  {{#if editable}} 
    <prose-mirror name="system.description" value="{{system.description}}" editable="{{editable}}" toggled="false">
      {{{enrichedDescriptionValue}}}
    </prose-mirror>
  {{else}} {{{enrichedDescriptionValue}}}
  {{/if}}
  </div>
</div>